# 算法训练记录

## 单题记录模板

```text
日期：
题目 ID：
LeetCode：
模式（训练后填写）：

首次观察：
暴力方案：
瓶颈：
候选优化：
状态：
不变量：

是否独立完成：
提示等级：
主要错因：

D+1：
D+3：
D+7：
```

---

## 2026-08-04

### P0-01 两数之和

```text
日期：2026-08-04
题目 ID：W0-D1-ALG-RESET-01 / P0-01
LeetCode：1. Two Sum
模式（训练后填写）：/tutor，A1 → A2
代码模式：Python ACM

首次观察：跳过暴力方案，直接回忆出“数值 → 下标”的哈希表最优解。
暴力方案：双循环枚举两个不同下标，时间 O(n^2)、额外空间 O(1)；本轮未先独立表达，由教学补齐。
瓶颈：暴力方案重复扫描后续元素寻找补数。
候选优化：遍历当前值 x 时，在 dict 中查询 target - x；未找到再写入 x → index。
状态：dict 保存当前下标之前已经出现的数值及其下标。
不变量：检查当前 x 时，dict 中只包含更早下标；命中补数即可保证两个下标不同。

是否独立完成：部分独立。核心哈希思路和最终 ACM 循环能写出；输入类型、复杂度理由、哈希表机制和均摊复杂度经过追问与讲解修正。
提示等级：A2
主要错因：
- 直接跳到记忆中的最优解，没有先建立暴力 → 瓶颈 → 优化链路；
- 一度把哈希查询误记为 O(n)，复杂度结论正确但理由错误；
- 初稿把 target 读成字符串；
- 边界表达最初只说“避免重复”，没有明确为“避免同一下标与自身配对”；
- 对哈希冲突、扩容、平均复杂度与均摊复杂度缺少机制理解。

当前状态：REVIEW，已通过有提示教学验收，尚未通过无提示复测。
D+1：2026-08-05，精简题面下口述状态/不变量并从空文件完成 ACM 实现。
D+3：2026-08-07，条件变化：只判断是否存在时改用 set，并说明为什么。
D+7：2026-08-11，/exam A0，完整实现、边界与复杂度说明。
```

### P0-02 存在重复元素

```text
日期：2026-08-04
题目 ID：W0-D2-ALG-RESET-01 / P0-02（较原计划提前）
LeetCode：217. Contains Duplicate
模式（训练后填写）：/tutor，A1
代码模式：Python ACM

首次观察：再次直接回答哈希表，未先给出题目要求的最直接方案。
暴力方案：双循环逐对比较，时间 O(n^2)、额外空间 O(1)。
瓶颈：同一元素会与多个后续元素重复比较。
候选优化：set 保存已经遍历过的数值；当前 x 已存在则输出 YES，否则加入集合。
状态：seen 保存当前位置之前出现过的不同数值。
不变量：检查 x 前不能先把 x 加入集合，否则当前元素一定会匹配自身。

是否独立完成：部分独立。set 核心思路、flag 方案和最终代码由用户写出；无重复分支、重复输出以及第一行 n 的 ACM 输入经过追问修正。
提示等级：A1
主要错因：
- 容易直接跳最优解，忽略暴力基线；
- 初稿在无重复时没有输出；
- 把循环后的 False 无条件输出，导致可能输出两次；
- 忘记读取题目规定的第一行 n；
- 输出曾使用 True/False，而题目协议要求 YES/NO。

当前状态：REVIEW，已通过有提示教学验收，尚未通过无提示复测。
D+1：2026-08-05，精简题面下独立完成 ACM 输入输出和 set 实现。
D+3：2026-08-07，比较双循环与 set 的时间/空间取舍。
D+7：2026-08-11，/exam A0，完整实现并覆盖有重复、无重复和首元素重复。
```

### P0-03 有效的字母异位词

```text
题目 ID：W0-D3-ALG-RESET-01 / P0-03
LeetCode：242. Valid Anagram
状态：READY
说明：2026-08-04 已展示完整题目卡，但尚未进入第一个分析问题，不计作一次正式 attempt，也不安排复测。
```
